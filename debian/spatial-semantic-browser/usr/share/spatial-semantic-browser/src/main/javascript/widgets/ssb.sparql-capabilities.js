/**
 * Methods for checking the geo capabilities of sparql endpoints.
 * 
 * TODO 
 * 
 */
var SparqlCapabilities = {
		hasVirtuosoRdf: {
			// Select * { ?s ?p ?o . Filter(datatype(?o) = virtrdf:Geometry) . }
		},

		hasGeorssLatLong: {
			//
		},
		
		hasGeoSparql: {
			
		}
	
}