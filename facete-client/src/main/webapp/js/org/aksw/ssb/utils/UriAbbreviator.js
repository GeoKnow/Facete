(function($) { 
	
	var ns = {};
	
	ns.UriAbbreviator = function() {
		var namespaces = {};
	};
	
	/**
	 * 
	 * //Returns { prefixes: { rdf: http://... }, entries: [ { prefix: rdf, name: type } ] }
	 *   
	 */
	ns.UriAbbreviator.prototype.abbreviateUri = function(uri) {
	};
	
	
	
	/**Split the UNRI
	 * 
	 * 
	 * 
	 *
	 */
	ns.listNamespaces = function(uri) {
		uri.split("#");
	};
	
		
		
})(jQuery);
