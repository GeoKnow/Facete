/**
 * This widget first retrieves the list of classes having instances with geographical features.
 * Upon choosing a class, the instances can be browsed
 * 
 */
//select { ?s a owl:Class . ?s rdfs:label ?l . Filter(langMatches(lang(?l), 'de')) . } order by ?s
